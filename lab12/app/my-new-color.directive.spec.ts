import { MyNewColorDirective } from './my-new-color.directive';

describe('MyNewColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MyNewColorDirective();
    expect(directive).toBeTruthy();
  });
});
